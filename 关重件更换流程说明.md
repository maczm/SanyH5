# 关重件更换（mom-key-component-change）流程说明

> **双重用途**：§1–§3 面向**使用者**（操作员 / 班组长 / 业务）；§4–§8 面向**开发者 / 维护者**。
> 字段级接口契约以 `API接口对接文档.md` §五 为准（本文不重复全部字段）；编码与协作规则以 `agent.md` 为准。
> 最后同步：修复「跨单污染 / 更换串位」等 9 项缺陷之后（commit `22a5e8c`）。

---

## 0. 术语与范围

| 术语 | 含义 |
|---|---|
| 关重件 | 需按序列号采集并追溯的关键零部件（电机、驱动桥、变速箱等） |
| 物料二维码 | `物料编码\|供应商\|序列号:数量`，三段均不可为空，数量为大于 0 的纯数字 |
| 关重件序号 `materialSeq` | **字符串**：`"1"`=前电机、`"2"`=后电机、`null`=无位置概念（页面不显示位置标签） |
| 订单类型 `wipOrderType` | `1`=生产订单、`2`=改制订单；页面所有订单号展示都带类型标注 |
| 满量 | 某关重件物料编码**已采集条数 ≥ 该物料配置数量之和** |
| 需采集数量 | `snList.length / Σ materialQty`（订单级）；卡片级为「该物料已采集条数 / 该物料 Σ materialQty」 |
| 需解绑数量 | `removeQty / needRemoveQty`，仅改制订单显示 |
| 页面形态 | 单页三视图（更换主页 / 移除页 / 更换页），纯静态 HTML+CSS+JS，嵌入 Portal iframe |

**业务范围**：本页负责关重件的**采集、解绑（移除旧件）、换件（移除旧件+保存新件）**三件事；不做质检判定、不做图片采集、不做装箱。

---

# 第一部分 · 使用者指导

## 1. 你会在什么场景用它

| 场景 | 订单类型 | 入口 | 涉及视图 |
|---|---|---|---|
| **正常采集**：把本车关重件序列号扫进系统 | 生产 / 改制 | 扫订单 → 逐个扫关重件 | 视图1 |
| **解绑**：改制订单要求拆下旧件，需先解绑原车关重件 | 仅改制订单 | 视图1「解绑」按钮 | 视图2 |
| **换件**：某关重件已采满，又扫到新的同款件 | 生产 / 改制 | 视图1 扫满量物料自动进入 | 视图3 |

> 一句话判断当前该做什么：**看「需采集数量」是否还有余量**。有余量→继续扫；满量还扫→进入换件；改制单上出现「解绑」→按需解绑。

## 2. 界面说明

### 2.1 视图1 · 关重件更换（主页）

| 界面元素 | 说明 |
|---|---|
| **订单号/VIN码** 输入框 | 手动键入或扫码枪直接键入；**按回车**即查询 |
| 🔍 **搜索按钮** | 手动输入后点它查询订单 |
| 📷 **扫码按钮** | 调起摄像头扫码（仅移动端；PC 提示「扫码功能仅在移动端可用」） |
| **订单号** | 查询成功后显示：`184000000012（生产订单）` / `184000000013（改制订单）` |
| **物料二维码** 输入框 | 扫码枪键入或手动输入；**按回车**即采集；程序化聚焦时不弹软键盘 |
| **需采集数量** | 例 `2/5`＝整车已采集 2 件 / 共需采集 5 件（未查订单时为空） |
| **需解绑数量**（仅改制订单） | 例 `1/2`＝已解绑 1 件 / 共需解绑 2 件；右侧「**解绑**」按钮仅在「还需解绑」时出现 |
| **关重件卡片** | 首行＝物料编码 + 物料描述 + 关重件类型，右侧＝`已采/需采`；下方每行一个序列号（序列号 / 前电机·后电机 / 录入时间 / 删除图标） |
| **完成** | 二次确认后清空本次会话（**不再调用任何后台接口**，只重置当前画面） |

卡片排序规则：**最近扫描的排最前**；没有序列号的卡片排最后。

### 2.2 视图2 · 关重件移除页（仅改制订单）

- 顶部显示「改制订单号（改制订单）」；
- 每条待移除明细：订单号（带类型）、物料编码、物料描述、旧序列号、录入时间，右上角「**移除**」；
- 底部「**返回**」回主页并刷新数据。

### 2.3 视图3 · 关重件更换页

- 顶部两行：`订单号-物料编码-物料描述`、`新序列号`（本次扫到的新件）；
- 每条待更换明细：订单号（带类型）、旧序列号、录入时间，右上角「**更换**」；
- 底部「**返回**」回主页。

### 2.4 弹窗

| 弹窗 | 触发 | 行为 |
|---|---|---|
| 加载中 | 每次接口调用 | 遮罩，接口返回或 10 秒超时自动消失 |
| 提示（Toast） | 校验失败 / 成功 | 非阻断，3 秒自动消失，也可点「确定」或遮罩关闭 |
| 二次确认 | 删除 / 移除 / 更换 / 返回 / 完成 | 「取消」不做任何事，「确定」执行 |
| 位置选择 | 前后电机无法自动判定 | 列出「前电机 / 后电机」，已采集的位置置灰不可点；点「取消」清空输入框 |

## 3. 标准作业流程

### 3.1 流程 A：正常采集

```
① 订单号/VIN 输入框 → 扫码或键入 → 回车（或点 🔍）
        ↓  接口：KC1 查订单 → KC2 查关重件
② 订单号后出现「（生产订单）/（改制订单）」标注；
   需采集数量出现 x/y；卡片按最近扫描时间列出
        ↓  自动聚焦「物料二维码」输入框
③ 扫关重件二维码 → 回车（或点 🔍 / 📷）
        ↓  页面校验：格式 → 是否本订单关重件 → 是否已采集 → 是否需要选前/后电机 → 是否满量
④ 采集成功：Toast「采集成功」，列表与数量自动刷新，输入框清空并保持聚焦
        ↓
⑤ 重复 ③ 直到「需采集数量」达到 x/x
⑥ 点「完成」→ 确认 → 会话清空，可开始下一单
```

**第 ③ 步的四种分支**

| 情况 | 页面表现 | 你要做什么 |
|---|---|---|
| 型号需要区分前后电机，且配置序号恰好是 1 和 2 | **自动分配**未采集的那个位置，不打扰你 | 无需操作，直接成功 |
| 型号需要区分前后电机，但序号不明确（如两条都是 1） | 弹「请选择关重件位置」 | 点「前电机」或「后电机」；已采过的位置点不动 |
| 该物料**已采满** | 不进视图1 列表，直接跳**视图3 更换页** | 在视图3 选一条要替换的旧件 →「更换」→ 确认 |
| 序列号之前已采过 | Toast「该序列号已采集」 | 核对是否扫重；输入框保留内容可修改 |

> 前后位置都已占满、但数量还没采满（例如某物料配置数量 3）时，页面会**弹位置选择让你人工指定**，不会卡住。

### 3.2 流程 B：改制订单解绑（视图2）

```
① 改制订单查询成功后，「需解绑数量」显示 1/2 这类进度
② 点「解绑」→ 进入视图2，列出待移除明细（可能来自多张单，行内订单号带类型）
③ 点某行「移除」→ 确认 → 旧件被解绑，Toast「移除成功」
        ↓  页面自动刷新：订单数据（进度、卡片）＋ 待移除清单
④ 重复 ③ 直到清单清空 / 进度达标
⑤ 点「返回」→ 回主页
```
- 解绑进度 `removeQty/needRemoveQty` 由后台返回，页面**不自行累加**；
- 当 `已解绑 ≥ 需解绑` 时，「解绑」按钮自动隐藏；
- **视图3 的换件移除不会推进解绑进度**（换的是新件位置，不是解绑任务），进度只由视图2 推进。

### 3.3 流程 C：换件（视图3）

```
① 扫到满量物料（或后台判定需更换：KC3 返回 isChange=1）→ 自动进入视图3
② 视图3 列出待更换旧件（按本次新件的物料编码过滤）
③ 选一条旧件 → 点「更换」→ 确认
        ↓  页面执行：KC4 移除旧件 → KC7 保存新件 → KC2 刷新
④ 成功：Toast「更换成功」→ 自动回主页，数量保持不超过需采总数
```

**如果第 ③ 步只成功了一半（旧件已移除、新件保存失败）——这是关键保护点**

| 你会看到 | 含义 | 怎么办 |
|---|---|---|
| Toast「移除成功，保存失败，请重试」+ 该行变成**「重试保存」**（整行变淡、左侧橙色） | 旧件已经从系统里解绑，新件还没写进去 | 点这条行的「**重试保存**」→ 只会补写新件，**不会重复移除** |
| 点**另外一条**旧件的「更换」被拦下，提示「旧件 X 已移除但新件未保存，请点「重试保存」」 | 防止新件挂到别人的旧件档案上 | 先回到那条「重试保存」把新件补上 |
| 点「返回」时弹确认「旧件 X 已移除，新件尚未保存，返回将丢失本次更换，是否继续？」 | 返回会放弃这次换件（旧件已删，新件不写） | 优先「取消」→ 点「重试保存」；确实要放弃才「确定」 |

### 3.4 提示语速查（用户看到 → 含义 → 处置）

| 提示文案 | 含义 | 处置 |
|---|---|---|
| 请输入订单号或VIN | 输入框为空 | 输入/扫码后重试 |
| 查询失败 + 后台原因 | KC1 失败 | 核对单号；网络问题稍后重试（**画面仍是上一单的数据，注意确认订单号标注**） |
| 加载失败 + 后台原因 | KC2/KC5/KC6 失败 | 关重件列表已清空，重查订单或重进视图 |
| 扫码功能仅在移动端可用，请手动输入 | PC/无摄像头 | 手动键入或扫码枪 |
| MAT-XXX 不是本订单关重件 | 二维码物料不在本单关重件配置里 | 核对物料与订单；也可能关重件清单未加载成功，重查订单 |
| 该序列号已采集 | 序列号重复 | 核对实体件；无需重复录入 |
| 二维码格式不正确（物料编码\|供应商\|序列号:数量） | 段数不对 / 有空段 / 数量非纯正数 | 重新扫码；二维码内容不含 `\|` 或数字异常时联系工艺 |
| 正在提交，请稍候 | 上一次提交还没返回（防重复） | 等待；不要连按回车 |
| 提交失败 + 后台原因 | KC3 失败 | 输入框保留原内容，可直接再回车重试 |
| 采集成功 / 移除成功 / 删除成功 / 更换成功 | 操作成功 | 继续下一件 |
| 移除失败 / 删除失败 + 后台原因 | KC4 失败 | 重试；连续失败联系信息化 |
| 移除成功，保存失败，请重试 | 换件半完成 | 点该行「重试保存」（见 §3.3） |
| 请求超时，请重试 | 接口 10 秒未回 | 重试；若后台其实已写入，核对列表后再决定是否重扫 |
| 接口未就绪，请稍后重试 | Portal 未注入该接口 | 退出页面重进；仍报错联系信息化 |
| 当前采集 2/3，尚未采集完成，确认结束本次会话？ | 未采满就点「完成」 | 「取消」继续采；确实结束才「确定」 |

### 3.5 误操作防护（当前版本已具备）

| 风险动作 | 页面保护 |
|---|---|
| 连按回车 / 扫码枪连扫同一张码 | 提交中忽略重复触发，只入库一次 |
| 换件途中误点「返回」 | 旧件已移除时二次确认，避免静默丢弃 |
| 换件失败后改选别的旧件 | 直接拦截，杜绝「新件挂错旧件档案」 |
| 未采满误点「完成」 | 二次确认 + 明确提示剩余数量 |
| 未查订单就扫物料 | 提示「请先查询订单信息」，不会误存 |
| 切换订单时关重件加载失败 | 列表清空，旧订单关重件不会被存进新订单 |
| 位置选择弹窗误点取消 | 自动清空输入框，避免残留内容被重复提交 |

### 3.6 扫码方式与设备注意

- **扫码枪**：输入框平时保持可编辑，可直接键入；回车即触发。连续扫码无需点输入框。
- **摄像头**：点 📷 调起 `OpenCamera`；结果自动回填并立即执行。仅移动端可用。
- **软键盘**：页面在「自动聚焦」物料输入框时会先临时只读再聚焦，因此**不会弹软键盘**；手动点击输入框照常弹键盘。
- 输入法组字中的回车不会误触发（中文输入法选词回车被忽略）。

---

# 第二部分 · 开发者指导

## 4. 架构与文件契约

### 4.1 部署形态（硬约束）

| 项 | 约定 |
|---|---|
| 生产文件 | **仅 3 个**：`index.html` / `index.js` / `index.css`（按 html/js/css 三段粘贴进 Portal 表单页） |
| 开发文件 | 额外 `mock.js`：本地兜底数据与 Mock 接口，**不部署**（Portal 不取）；`index.html` 中置于业务 JS 之前 |
| 依赖 | 根目录 `../jquery.min.js`（jQuery 3.4.0，随解决方案整体部署） |
| 宿主通信 | Portal 在 iframe 加载前注入 `window.KeyComponentChange_*` 七个函数；页面用 `typeof window.xxx === "function"` 之外的方式**不做检测**，由 `apiCall` 统一守卫 |
| 回车拦截 | 页面顶部定义空实现 `function Portal_OnDocumentKeyDown() {}`（Portal 约定钩子，定义即生效，页面不调用） |
| 禁用 id | 全页零 `id`，元素定位/事件全部用 class + 委托 |

### 4.2 模块结构（`KeyComponentChange` 命名空间，按职责分组）

| 分组 | 成员 |
|---|---|
| 配置常量 | `API_TIMEOUT`(10s)、`BUTTON_SWITCH`、`BUTTON_SELECTOR`、`state` |
| 基础设施 | `cloneTemplate` / `apiCall` / `buildTaskRequest` / `now` / `normalizeTime` / `formatOrderNo` / `getOrderTypeLabel` / `getMotorPositionLabel` |
| 消息与遮罩 | `showToast` / `hideToast` / `showLoading` / `hideLoading` / `showConfirmDialog` |
| 扫码与输入 | `doScan` / `focusMaterialInput` / `clearMaterialInput` / `setInputSource` |
| 按钮开关 | `applyButtonSwitch` / `updateUnbindButton` |
| 视图切换 | `switchView` / `enterRemoveView` / `enterChangeView` / `backToCheckView` / `leaveChangeState` |
| 视图1 订单 | `getOrderSearchPayload` / `queryOrderInfo` / `clearKeyComponentState` / `loadKeyComponentInfo` |
| 视图1 列表 | `sumMaterialQty` / `compareSerialByScanTime` / `compareGroupByLatestScanTime` / `groupKeyComponentList` / `renderKeyComponentList` |
| 视图1 采集 | `parseMaterialQrCode` / `findMatchedComponents` / `getCollectedSequenceList` / `getMaterialQuantityState` / `isSerialCollected` / `resolveMaterialSequence` / `getMotorSequenceList` / `showMotorPicker` / `handleMaterialCheck` / `findComponentBySequence` / `buildPendingScan` / `checkAndSave` / `startMaterialChange` / `buildCheckReported` / `submitCheckAndSave` / `removeSerial` |
| 会话 | `completeCheck` / `resetCheckSession` |
| 视图2 | `loadRemoveRecordList` / `renderRemoveRecordList` / `removeRecord` |
| 视图3 | `loadChangeRecordList` / `renderChangeRecordList` / `markChangeRecordPendingSave` / `changeRecord` / `saveChangedKeyComponent` |
| 页面 | `fitPageHeight` / `initEvents` / `initPage` |

### 4.3 视图与 DOM

三个视图容器（互斥显隐，`.hidden` 切换）：

| 视图 | 容器 class | 关键子元素 |
|---|---|---|
| 视图1 主页 | `.key-component-check-view` | `.input-order-key`、`.input-material-qr`、`.order-no-tag`、`.collect-quantity-tag`、`.remove-quantity-tag`、`.remove-quantity-row`、`.btn-unbind`、`.key-component-list`、`.empty-key-component`、`.btn-complete` |
| 视图2 移除页 | `.key-component-remove-view` | `.remove-order-no-tag`、`.remove-record-list`、`.empty-remove-record`、`.btn-back-remove` |
| 视图3 更换页 | `.key-component-change-view` | `.change-material-tag`、`.new-serial-tag`、`.change-record-list`、`.empty-change-record`、`.btn-back-change` |

模板（`<template>` 单根结构，`cloneTemplate` 取 `firstElementChild`）：

| 模板 class | 用途 |
|---|---|
| `.template-key-component-card` | 视图1 关重件卡片 |
| `.template-serial-row` | 卡片内序列号行 |
| `.template-remove-record-card` | 视图2 明细卡 |
| `.template-change-record-card` | 视图3 明细卡 |
| `.template-motor-option` | 位置选择项 |

常驻弹窗骨架：`.template-loading` / `.template-toast` / `.template-confirm` / `.template-motor-picker`。

### 4.4 `state` 字段表

| 字段 | 初值 | 含义 / 生命周期 |
|---|---|---|
| `orderInfo` | `null` | KC1 返回的订单对象；换单成功即替换；「完成」置空 |
| `keyComponentList` | `[]` | KC2 的关重件配置；换单/KC2 失败时清空 |
| `serialList` | `[]` | KC2 的已采集序列号 |
| `removeQty` / `needRemoveQty` | `0` | 解绑进度（后台返回值，页面不累加） |
| `inputSource` | `{inputType:"手输", inputCode:13}` | 上报的输入方式：回车=手输/13、搜索按钮=手输/1、扫码按钮=扫码/1 |
| `pendingScan` | `null` | **换件决策中的本次扫描**（订单物料 + 关重件 + 输入方式）；KC3 返回 `isChange=1` 或满量时保留，保存成功/返回/失败/完成后清空；同时用作防重复进入视图3 的标志 |
| `changedOldGenealogyId` | `null` | KC4 返回的被删数据 ID，KC7 回传；**非空 = 存在"旧件已移除、新件未保存"的半完成状态** |
| `changedOldSerialNo` | `null` | 半完成状态对应的旧件序列号（用于重试绑定与提示文案） |
| `isSubmitting` | `false` | KC3/KC7 在途锁（防重复提交） |
| `orderRequestSequence` | `0` | KC1 请求序号（丢弃过期响应） |
| `keyComponentRequestSequence` | `0` | KC2 请求序号（丢弃过期响应） |

模块级：`_loadingCount`（遮罩计数，归零才隐藏）、`_toastTimer`（Toast 定时器）、`_initRetryCount`（容器等待重试次数）。

### 4.5 按钮开关（agent.md §8.11 落地）

```js
BUTTON_SWITCH  // 每个按钮：{ visible: 显示开关, permitted: 权限开关 }
  searchOrder / scanOrder / searchMaterial / scanMaterial / unbind
  complete / deleteSerial / removeRecord / changeRecord / back

BUTTON_SELECTOR // 开关项 → class 选择器（back 同时管 .btn-back-remove, .btn-back-change）
```

- `applyButtonSwitch()`：遍历映射表，`visible:false → addClass("hidden")`、`permitted:false → prop("disabled") + addClass("disabled")`；**唯一例外 `unbind`**——它不参与 hidden 切换，显示与否由 `updateUnbindButton()` 按业务条件与开关取交集决定；
- **每次渲染后必须重放**：`renderKeyComponentList` / `renderRemoveRecordList` / `renderChangeRecordList` 末尾都会调用，否则克隆行拿不到开关状态；
- 业务条件写法（解绑按钮）：`wipOrderType === "2" && needRemoveQty > removeQty && BUTTON_SWITCH.unbind.visible`。

## 5. 状态机

### 5.1 视图状态机

```
                 ┌──────────────────────────────┐
                 │ 视图1 更换主页                │
                 │ (.key-component-check-view)   │
                 └───┬───────────┬───────────┬───┘
      解绑按钮(改制且还需解绑)│   满量扫描/      │ 完成(二次确认)
                           │   isChange=1     │
                           ▼                  ▼
              ┌────────────────────┐   ┌────────────────────┐
              │ 视图2 移除页        │   │ 视图3 更换页        │
              │ 返回→视图1(+KC2)    │   │ 返回→(半完成则确认) │
              └────────────────────┘   │ →视图1(+KC2)        │
                                       └─────────┬──────────┘
                                       更换成功→视图1(+KC2)
```

| 迁移 | 触发 | 前置条件 | 副作用 |
|---|---|---|---|
| 视图1 → 视图2 | 点「解绑」 | `orderInfo.wipOrderType === "2" && needRemoveQty > removeQty && 开关可见` | 渲染改制订单号 + KC5 |
| 视图1 → 视图3 | 扫到满量物料 / KC3 返回 `isChange=1` / 「更换」流程中 | `orderInfo` 与 `pendingScan` 均存在 | 渲染头部 + KC6 |
| 视图2 → 视图1 | 点「返回」 | — | 清 `pendingScan`/`changedOld*`/`isSubmitting`，KC2 |
| 视图3 → 视图1 | 点「返回」 | `changedOldGenealogyId` 为空 | 同上 |
| 视图3 → 视图1（确认后） | 点「返回」+ 确认 | 半完成状态 | 同上（**放弃本次换件**） |
| 视图3 → 视图1 | KC7 成功 | — | `pendingScan`/`changedOld*` 清空 + KC2 + 清空并聚焦输入框 |
| 任意 → 视图1（空态） | 点「完成」+ 确认 | — | `resetCheckSession()`：清 state、清单、数量标签、请求序号 + 聚焦订单输入 |

### 5.2 `pendingScan` 生命周期（换件的核心）

```
handleMaterialCheck()
   ├─ 未满量 → checkAndSave(): pendingScan = 本次扫描 → KC3
   │        ├─ res.code !== 0        → pendingScan = null（可重新扫）
   │        ├─ isChange !== "1"      → pendingScan = null → KC2 刷新
   │        └─ isChange === "1"      → 保留 pendingScan → enterChangeView()
   └─ 已满量 → startMaterialChange(): pendingScan 为空才写入 → enterChangeView()
                                        （pendingScan 非空说明已在换件决策中，直接忽略重复触发）
视图3:
   KC4 成功 → changedOldGenealogyId / changedOldSerialNo 落库到 state，行标记「重试保存」
   KC7 成功 → pendingScan / changedOldGenealogyId / changedOldSerialNo 全部清空
   KC7 失败 → 三者保留（半完成态），允许「重试保存」
   返回（半完成）→ 二次确认 → leaveChangeState() 清空三者
```

### 5.3 请求并发控制

| 机制 | 实现 | 防的问题 |
|---|---|---|
| 提交在途锁 | `state.isSubmitting`（`submitCheckAndSave` / `saveChangedKeyComponent` 入口置位，回调复位；`handleMaterialCheck` 入口拦截） | 连按回车、扫码枪连扫导致的重复 KC3 / 重复 KC7 |
| 请求序号 | `orderRequestSequence`（KC1）、`keyComponentRequestSequence`（KC2）：请求前自增并捕获，回调先 `hideLoading()` 再比对，不等则 **丢弃** | 慢的旧响应覆盖新数据（换件后 KC2 回退、连查两单串单） |
| 遮罩计数 | `_loadingCount` 递增/递减，归零才隐藏；**丢弃响应也必须先 `hideLoading()`** | 并发请求时遮罩提前消失/永久残留 |
| 重复进入保护 | `startMaterialChange` 检查 `pendingScan` | 重复进入视图3、拉两份清单 |

## 6. 接口调用时序

### 6.1 序列 A：查单 + 正常采集

```
操作员                页面                        Portal 注入函数
  │  输入/扫码订单 ───▶ queryOrderInfo()
  │                     ├─ 解析：纯数字→wipOrderNo；17位非纯数字→vin；其它→wipOrderNo
  │                     ├─ showLoading("查询订单中...")
  │                     ├─ KC1 ────────────────▶ KeyComponentChange_GetWipOrderNoInfo
  │                     │   （序号守卫：过期响应丢弃）
  │                     ├─ hideLoading
  │                     ├─ 失败→Toast「查询失败」+ 画面保持上一单（orderInfo 不变，数据自洽）
  │                     └─ 成功→state.orderInfo = data
  │                          ├─ clearKeyComponentState()   ← 换单即清空上一单关重件数据
  │                          ├─ 订单号标签带类型标注、清空物料输入框
  │                          └─ loadKeyComponentInfo(cb)
  │                                ├─ showLoading("加载关重件中...") → KC2
  │                                ├─ 失败→clearKeyComponentState() + Toast「加载失败」
  │                                └─ 成功→写入 keyComponentList/serialList/removeQty/needRemoveQty
  │                                        → renderKeyComponentList() → applyButtonSwitch()
  │                                        → cb: 聚焦物料输入框（临时只读，不弹键盘）
  │  扫物料 + 回车 ───▶ handleMaterialCheck()
  │                     ├─ 在途锁？「正在提交，请稍候」
  │                     ├─ 有订单？→ 有内容？→ 格式合法（3段/非空/数量纯正数）？
  │                     ├─ 是本订单关重件？（按 materialNo 匹配配置）
  │                     ├─ 序列号未采过？（state.serialList）
  │                     ├─ 满量判定：已采条数 ≥ Σ materialQty ?
  │                     └─ resolveMaterialSequence() → 序号 → buildPendingScan()
  │                          ├─ 未满量 → KC3 CheckAndSave(reported)
  │                          │     ├─ 失败→Toast「提交失败」+ pendingScan 清空
  │                          │     ├─ isChange==="1"→ 进视图3
  │                          │     └─ 其它→Toast「采集成功」→ KC2 → 清空+聚焦输入框
  │                          └─ 已满量 → KC4/KC7 走视图3（见序列 C）
```

### 6.2 序列 B：解绑（视图2）

```
点「解绑」 ─▶ enterRemoveView()
              ├─ 头部写「改制订单号（改制订单）」
              ├─ switchView("key-component-remove-view")
              └─ loadRemoveRecordList() → KC5 → renderRemoveRecordList → applyButtonSwitch
点「移除」 ─▶ removeRecord(record) → 二次确认
              ├─ KC4 Remove{ wipOrderNo, wipOrderType, serialNo, materialSerialNo }（行内订单，可能非当前单）
              ├─ 失败→Toast「移除失败」
              └─ 成功→Toast「移除成功」→ loadKeyComponentInfo()（进度+卡片） + loadRemoveRecordList()（清单）
点「返回」 ─▶ backToCheckView()（无半完成状态）→ 视图1 + KC2
```

### 6.3 序列 C：换件（视图3，含失败重试）

```
① 进入：满量扫描 / KC3 返回 isChange=1
   enterChangeView()
     ├─ 头部：订单号-物料编码-物料描述 / 新序列号
     ├─ switchView("key-component-change-view")
     └─ loadChangeRecordList() → KC6{ wipOrderNo, wipOrderType, materialNo } → 渲染 2 条示例明细

② 首次更换：点某行「更换」→ 二次确认「确认将旧序列号 X 更换为 Y ？」
     ├─ KC4 Remove{ 行内 wipOrderNo/serialNo, record.materialSerialNo }
     │    ├─ 失败 → Toast「移除失败」，无残留状态，可重选
     │    └─ 成功 → state.changedOldGenealogyId = data.oldGenealogyID
     │              state.changedOldSerialNo = record.materialSerialNo
     │              markChangeRecordPendingSave()   ← 该行变淡 + 按钮文案「重试保存」
     │              loadKeyComponentInfo()          ← 先刷新订单数据（序号 N）
     │              saveChangedKeyComponent()       ← 紧接着 KC7（序号 N+1）
     │                   ├─ 失败 → Toast「移除成功，保存失败，请重试」；保留三者，停留视图3
     │                   └─ 成功 → 清 pendingScan/changedOld* → Toast「更换成功」
     │                            switchView(视图1) → KC2 → 清空并聚焦输入框

③ 重试（半完成态）：
     点「重试保存」（同一行） ─▶ 命中 changedOldGenealogyId 且序列号一致
                                  → saveChangedKeyComponent()：只发 KC7（**不再 KC4**）
     点其它行「更换」         ─▶ 拦截 + Toast「旧件 X 已移除但新件未保存，请点「重试保存」」
     点「返回」               ─▶ 二次确认「…返回将丢失本次更换，是否继续？」
                                  ├─ 取消 → 留在视图3，状态不变
                                  └─ 确定 → leaveChangeState() → 视图1 + KC2

④ 竞态保护：② 中 KC2(N) 与 KC2(N+1) 重叠时，序号守卫保证只有 N+1 的结果会渲染，
   慢回的旧数据（不含新件、且含已置换旧件）不会覆盖界面。
```

### 6.4 接口调用时机总表

| 接口 | 触发动作 | 关键入参 | 出参用途 | 失败处理 |
|---|---|---|---|---|
| KC1 `GetWipOrderNoInfo` | 订单输入框回车 / 🔍 / 📷 | `wipOrderNo` 或 `vin` | `orderInfo`（订单号、类型、主机物料） | Toast「查询失败」，画面不变 |
| KC2 `GetKeyComponentInfo` | KC1 成功后；删除序列号后；解绑/换件后；返回视图1 时 | `wipOrderNo`、`wipOrderType` | 配置 + 已采序列号 + 解绑进度 | 清空列表 + Toast「加载失败」 |
| KC3 `CheckAndSave` | 未满量的关重件扫码 | 订单物料 + 关重件 + 二维码 + 输入方式 | `isChange` | Toast「提交失败」，可重扫 |
| KC4 `Remove` | ①视图1 删除序列号 ②视图2 移除 ③视图3 更换（移除旧件） | `wipOrderNo`、`wipOrderType`、`serialNo`、`materialSerialNo` | `oldGenealogyID`（仅 ③ 使用） | Toast「删除失败/移除失败」 |
| KC5 `GetRemoveKeyComponentInfo` | 进入视图2；移除成功后 | `wipOrderNo`、`wipOrderType` | 待移除明细数组 | Toast「加载失败」 |
| KC6 `GetChangeKeyComponentInfo` | 进入视图3 | `wipOrderNo`、`wipOrderType`、`materialNo` | 待更换明细数组 | Toast「加载失败」 |
| KC7 `Save` | 视图3 更换确认后 / 重试保存 | KC3 全字段 + `oldGenealogyID` | 无 | Toast「移除成功，保存失败，请重试」 |

> 入参统一信封：`{ taskType: "<英文任务名>", reported: {...} }`（`buildTaskRequest`）。
> 时限：所有接口 `API_TIMEOUT = 10000ms`，超时按 `{code:-1}` 处理，走各自的失败分支。

## 7. 关键实现约定（改代码前必读）

| 约定 | 说明 |
|---|---|
| **零 id** | 选择器只用 class；`<template>` 也用 class，`cloneTemplate` 内部 `document.querySelector("." + className)` |
| **模板单根** | `cloneTemplate` 取 `fragment.firstElementChild`；模板必须单根，否则 `.data()` 丢失 |
| **零 HTML 拼接** | 全用 `.text()/.val()/.attr()` 赋值（天然防 XSS，无需转义函数） |
| **事件委托一次性** | `initEvents()` 在 init 时绑定，动态克隆元素靠 class 委托；弹窗回调数据挂 `.data()` |
| **`.hidden` 不用 `show()/hide()`** | 统一 `addClass/removeClass/toggleClass("hidden")`（CSS 为 `display:none !important`） |
| **回车统一入口** | 根容器委托 `keydown` → 按输入框 class 分派；跳过输入法组字（`isComposing` / `keyCode===229`） |
| **禁失焦触发** | 业务动作只由 回车/按钮/扫码 触发（点击按钮会先失焦，blur 触发会与点击抢事件） |
| **扫码框聚焦** | 程序化聚焦前临时 `readonly`（不弹软键盘），150ms 后解锁；手动点击照常弹键盘 |
| **按钮不夺焦** | 根容器 `mousedown` 上对 `button` 做 `preventDefault()` |
| **初始显隐显式执行** | `initPage()` 里跑一次 `applyButtonSwitch()` + `renderKeyComponentList()` |
| **数量标签空态** | 未查订单时数量标签留空（不显示 `0/0`），避免误读为"该单需采 0 件" |
| **等高自适应** | `fitPageHeight()`：`max(240, 视口高 - 容器顶部)`，`resize` 重算，避免整页滚动 |
| **运行时新增字段** | 改 `state` 必须同步在 `resetCheckSession()` / `leaveChangeState()` 里复位，否则跨会话残留 |

## 8. 维护指导

### 8.1 常见扩展场景

| 需求 | 改哪里 | 注意 |
|---|---|---|
| 新增/调整按钮的显示或权限 | `BUTTON_SWITCH` + `BUTTON_SELECTOR`（一处配置，`applyButtonSwitch` 统一应用） | 克隆行需在渲染后重放；业务条件单独写 `updateXxxButton()` |
| 新增一个输入框 + 搜索/扫码按钮 | HTML 骨架（`.btn-search-*` / `.btn-scan-*`）+ `initEvents` 委托 + `setInputSource` | 必须成对提供搜索与扫码按钮；回车走统一入口 |
| 新增列表字段 | `<template>` 加元素 → `renderXxxList` 里 `.text()` 赋值 | 三处同步：模板 / 渲染 / 回归断言 |
| 新增接口 | `API接口对接文档.md` 先写契约 → `mock.js` 加 Mock → 页面加调用（走 `apiCall` + 序号/在途锁） → 回归补断言 | 顺序：文档 → Mock → 页面 → 测试 |
| 调整满量/序号规则 | `resolveMaterialSequence` / `getMaterialQuantityState` / `findComponentBySequence` | 规则改动必须同步接口文档 §五 业务约定 |
| 调整保存失败重试语义 | `state.changedOld*` 三者 + `changeRecord` 守卫 + `backToCheckView` 确认 | 三者是"半完成态"的完整表达，勿只改其一 |

### 8.2 本地联调与回归

```bash
cd /home/wangzm/projects/SanyH5

# 静态检查（0 error 0 warning 为通过）
eslint mom-key-component-change/index.js mom-key-component-change/mock.js

# 单页回归（前置：nginx 8080；页面 http://127.0.0.1:8080/SanyH5/mom-key-component-change/）
NODE_PATH=$(npm root -g) node tests/key-component-change.regress.cjs

# 全量回归（并行执行器）
NODE_PATH=$(npm root -g) node tests/run-all.regress.cjs
```

**本地 Mock 订单（`mock.js`）**

| 订单号 / VIN | 类型 | 用途 |
|---|---|---|
| `184000000012` / `LSVU2A0N260800001` | 生产 | 5 件配置：电机 ×2（序号 1/2，自动分配）、驱动桥 ×1（序号 null）、变速箱 ×2（序号 3）；含已采序列号 |
| `184000000013` / `LSVU2A0N260800002` | 改制 | 电机两条配置序号都是 `"1"`（触发位置弹窗）；需解绑 2 件 |
| `184000000001/2`（待移除明细） | 生产 | 视图2 的待移除数据（含跨单场景：行内订单 ≠ 当前单） |
| `mockChangeRecordList` | 混合 | 视图3 待更换数据；`CHG-` 前缀序列号会触发 `isChange=1` |
| 回归脚本临时注入：`...097`（防重）、`...098`（同码多配置）、`...099`（位置用尽 2+1=3） | 生产 | 仅测试用，写在 `tests/key-component-change.regress.cjs` 内 |

**写回归脚本的约定**
- 断言等待用「状态等待」：`waitIdle()` 等 loading 遮罩消失，禁止固定 `sleep`；
- 可用 `page.addInitScript(() => { window.__mockDelayMilliseconds = 60 })` 压低 Mock 延迟；
- 需要"半完成/失败"场景时，用 `page.evaluate` 临时包装 `window.KeyComponentChange_Save` 等（**记得还原**）；
- 每个断言对应一个用户可观察行为，避免只断言内部变量。

### 8.3 提交前自检（DoD 摘要，完整见 agent.md §10）

- [ ] `eslint` 0 error 0 warning
- [ ] 本页回归全绿 + 0 JS 错误（结构变更先同步回归脚本）
- [ ] 零 id / 零 HTML 拼接 / 模板单根 / `.hidden` 用法
- [ ] 按钮开关集中配置，克隆行渲染后重放
- [ ] Mock 数据只在 `mock.js`，业务 JS 零内联
- [ ] 接口契约变更先改 `API接口对接文档.md`
- [ ] 新增/变更的流程分支已写入本文件

### 8.4 已知边界与待决策

| 项 | 现状 | 影响 | 建议 |
|---|---|---|---|
| KC5/KC6 无请求序号保护 | 与 KC2 同类竞态（重叠请求可能回填过期清单） | 触发需"返回后立刻重进"，概率低；清单每次进入都会重拉 | 如要加固，套用 `keyComponentRequestSequence` 同一机制 |
| 订单查询失败保留上一单画面 | `orderInfo` 未变，数据自洽；回归已固化该行为 | 操作员可能误扫到旧单（订单号标注仍在，可识别） | 若业务要求"失败即清屏"，需同时改页面、回归与文档 |
| `initPage` 容器等待上限 5s | 100 × 50ms 后静默放弃初始化 | Portal 极慢注入时白屏且无提示 | 可延长或加超时提示 |
| 「完成」不落任何后台数据 | 接口清单中无完成类 API，纯本地会话重置 | 操作员可能误以为"已提交" | 若业务需要完成动作，需新增接口并改文案/文档 |
| 解绑进度只随视图2 推进 | 由后台 `removeQty` 决定，页面不累加（Mock 语义：换件不推进） | 影响业务口径理解 | 与后台确认后固化到接口文档 |

---

## 附录 A · 接口速查（详见 `API接口对接文档.md` §五）

| 编号 | 函数 | taskType | 用途 |
|---|---|---|---|
| KC1 | `KeyComponentChange_GetWipOrderNoInfo` | `GetWipOrderNoInfo` | 查订单（订单号或 VIN 二选一） |
| KC2 | `KeyComponentChange_GetKeyComponentInfo` | `GetKeyComponentInfo` | 关重件配置 + 已采序列号 + 解绑进度 |
| KC3 | `KeyComponentChange_CheckAndSave` | `CheckAndSave` | 校验并保存扫描件（`isChange="1"` → 走换件） |
| KC4 | `KeyComponentChange_Remove` | `Remove` | 移除/删除关重件，返回 `oldGenealogyID` |
| KC5 | `KeyComponentChange_GetRemoveKeyComponentInfo` | `GetRemoveKeyComponentInfo` | 待移除明细 |
| KC6 | `KeyComponentChange_GetChangeKeyComponentInfo` | `GetChangeKeyComponentInfo` | 待更换明细（按 `materialNo` 过滤） |
| KC7 | `KeyComponentChange_Save` | `Save` | KC3 全字段 + `oldGenealogyID` 保存新件 |

## 附录 B · 页面文案总表（回归断言可直接引用）

**Toast**：请输入订单号或VIN ｜ 查询失败 ｜ 加载失败 ｜ 扫码功能仅在移动端可用，请手动输入 ｜ 正在提交，请稍候 ｜ 请先查询订单信息 ｜ 请输入或扫码物料二维码 ｜ 二维码格式不正确（物料编码|供应商|序列号:数量） ｜ {物料编码} 不是本订单关重件 ｜ 该序列号已采集 ｜ 前/后电机均已采集 ｜ 提交失败 ｜ 采集成功 ｜ 删除失败 / 删除成功 ｜ 移除失败 / 移除成功 ｜ 保存失败 ｜ 移除成功，保存失败，请重试 ｜ 更换成功 ｜ 旧件 {X} 已移除但新件未保存，请点「重试保存」 ｜ 接口未就绪，请稍后重试 ｜ 请求超时，请重试

**二次确认**：确认删除关重件序列号 {X} ？ ｜ 确认移除关重件旧序列号 {X} ？ ｜ 确认将旧序列号 {X} 更换为 {Y} ？ ｜ 旧件 {X} 已移除，新件尚未保存，返回将丢失本次更换，是否继续？ ｜ 当前采集 {c}/{r}，尚未采集完成，确认结束本次会话？ ｜ 确认完成并清空当前会话？

**Loading 文案**：查询订单中... ｜ 加载关重件中... ｜ 提交中... ｜ 删除中... ｜ 加载移除明细中... ｜ 移除中... ｜ 加载更换明细中... ｜ 移除旧件中... ｜ 保存新件中...

## 附录 C · 一次换件的完整数据流（备忘）

```
扫码 → parseMaterialQrCode      : { materialNo, partner, materialSerialNo, materialQty }
     → findMatchedComponents    : 配置条目[]
     → getMaterialQuantityState : { collectedQuantity, requiredQuantity }
     → resolveMaterialSequence  : materialSeq（自动分配 / 人工选择）
     → findComponentBySequence  : 对应配置条目（决定 materialID / uomCode）
     → buildPendingScan         : 本次扫描上下文（含 inputType/inputCode）
     → buildCheckReported       : KC3/KC7 共用报文 = 订单物料 + 关重件 + 二维码 + 输入方式
     → KC4 → oldGenealogyID     : 被替换旧件 ID
     → KC7(reported + oldGenealogyID) → KC2 刷新 → 会话回到视图1
```
