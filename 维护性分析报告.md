# SanyH5 维护性分析报告

> 分析日期：2026-08-10（第一版）；2026-08-10（修订版，纳入部署约束）
> 范围：全部现有代码（mom-cert / mom-packing / mom-nameplate-photo-upload），仅分析，未改动任何代码。
> 说明：tmp.cs / tmp.json 为后端临时文件，不在分析范围内。

---

## 1. 项目概况与总体判断

| 维度 | 现状 |
|---|---|
| 技术栈 | 纯静态 HTML/CSS/JS，无 package.json、无构建工具（webpack/vite/gulp 均无） |
| 唯一依赖 | jQuery 3.4.0（三个页面均通过 `cdn.jsdelivr.net` CDN 引入） |
| 代码规模 | JS 3 个文件共 3,236 行；HTML 最大为 mom-cert/index.html（4,675 行，含约 533 行内联脚本）；CSS 共 2,029 行 |
| 版本管理 | **非 git 仓库，无任何版本历史** |
| 模块化 | 无（无 ES Module / IIFE / 命名空间，全部为全局脚本） |
| 架构模式 | 3 个互相独立的页面，嵌入 MOM/Portal 平台 iframe；Portal 注入 `window.xxx` API + 本地 Mock 兜底（`typeof window.xxx != "function"` 检测） |

**部署约束（重要）**：生产环境为纯 H5，每页**只能有 index.html / index.js / index.css 三个文件**；不能用 IIFE；不能新增公共文件。所有改进方案必须在此约束内进行。
**mom-cert 的 index.html 不允许修改**（非本人维护，平台方模板），只能改 index.js。好消息：index.js 在内联脚本**之前**加载（index.html:11 vs 4143），且内联脚本全部逻辑都在 `$(document).ready` 里执行——因此 JS 侧可"叠加"修复，详见 2.1。

**总体判断**：代码底子尚可，新项目（packing / nameplate）具备 XSS 转义、事件委托、Mock 机制、CSS 变量作用域隔离等良好意识；但存在三类真问题——① **存在真实 bug，且最危险的一类是"本地 Mock 测不出来、生产必现"**；② 跨项目复制粘贴；③ mom-cert 的历史遗留代码（内联一切 + 调试残留）。

**核心问题结论**：

- **有没有 bug**：有，详见第 2 节 Bug 清单。高危项集中在 mom-cert（`debugger;`、XSS、裸 `JSON.parse`、`__DEV__` 开关），新页面主要是异步竞态（照片串单、重复提交）。
- **结构需不需要改变**：需要小改，不需要大动。在 3 文件约束内，页内用模板字符串 + 函数归组/前缀即可，不值得引入构建工具链。
- **全部在 JS 里好不好维护**：「用 JS 渲染 UI」本身不是问题（React/Vue 本质也是 JS 渲染），真正的维护成本来自**字符串拼接 HTML、全局函数裸奔、复制粘贴、两套代码风格共存**——详见第 4 节。

---

## 2. 实际 Bug 清单（按页）

### 2.1 mom-cert（合格证查看页）——问题最集中

> **约束：index.html 不能动，只能改 index.js。**
> 时序依据：index.js 在内联脚本之前加载（index.html:11 vs 4143）；内联脚本所有逻辑在 `$(document).ready` 中执行，且 ready 回调按注册顺序执行 → index.js 顶层代码可在内联脚本读取 `$Context.inputs` **之前**做归一化，ready 轮询完成后可做 DOM 补救。

#### 🔴 高——可用 index.js 叠加修复

| # | Bug | 位置 | JS 侧修复方案 |
|---|---|---|---|
| 1 | **裸 `JSON.parse` 无防御（约 14 处）** | index.html:4260, 4272, 4306, 4319, 4432, 4450, 4550, 4556, 4573, 4584, 4601, 4607, 4624, 4630 | 内联脚本的守卫是 `if (x != '')`，挡不住 `undefined`。index.js **顶层归一化** `$Context.inputs`：可选 JSON 字段缺失补 `''`（`CHECK_CONTENT` 补 `'[]'`，因 isCertSuccess 无守卫）、`MSG` 补 `''`、显示字符串补 `''`。一次归一化消除全部崩点 |
| 2 | **XSS：未转义 HTML 注入** | index.html:4491, 4504 | 内联渲染后由 index.js 对 `#check_content` **后置消毒**：剔除 `script`/`iframe`/`object`/`style` 及全部 `on*` 属性、`javascript:` 链接，保留 `<p><span>` 结构与内联样式 |
| 3 | `($Context.inputs.MSG).split("*****")` | index.html:4499 | 同上，MSG 归一化为 `''` 后安全 |
| 4 | **`__DEV__` 硬编码 `true`** | index.js:10（就在 index.js 内） | 改为运行时判断（URL 带 dev 参数或由 Portal 注入开关），部署不再改代码 |

#### 🟡 中——可用 index.js 叠加修复（可选）

| # | Bug | 位置 | JS 侧修复方案 |
|---|---|---|---|
| 5 | `formatDate` 老 iOS 解析失败显示 "NaN年NaN月" | index.html:4146-4148 | 渲染后若文本含 "NaN"，用 `$Context.inputs.ISSUEDATE/PRODUCTTIME` 原值重算日期替换（工业平板多为 Android WebView，可不做） |

#### ⚪ HTML 内不可动——按约束忽略

| # | 问题 | 说明 |
|---|---|---|
| 6 | `debugger;` ×3（index.html:4257, 4388, 4447） | `debugger` 是语言关键字，JS 无法禁用。Chrome 仅**打开 DevTools 时**才暂停——生产移动端无 DevTools 无影响，桌面排查时手动忽略 |
| 7 | `reloadZHC` 45 对 hide/show、`loadHBAndRYData` 4 分支复制 | 在 HTML 内，不动 |
| 8 | 175 处内联 `style="display:none"`、`==` 弱比较 | 在 HTML 内，不动 |
| 9 | 内联旧脚本 vs index.js 新引擎两套风格 | 结构性问题，HTML 不可动则共存，改校验逻辑只改 index.js 侧 |

### 2.2 mom-packing（装箱作业页）

#### 🔴 高

| # | Bug | 位置 | 后果 |
|---|---|---|---|
| 1 | **上传竞态：照片串单** | handleFileSelect（781-811）+ removePackingPanel（731-737） | 上传是异步的（压缩→上传→回调）。回调返回前用户搜了别的单/选了别的卡片，回调把旧照片 push 进新状态 → **选 A 单提交了 B 单的照片** |
| 2 | **重复提交窗口** | handleSubmit（996 行回调里立即 `state.submitting=false` + 按钮恢复） | 成功 toast 3 秒内按钮可再点，同一单可重复提交；`showToast` 开头 `$(".toast-mask").remove()`（251-252）直接移除旧 toast 不触发其回调——第二次提交失败时第一次的"成功重置"丢失 |

#### 🟡 中

| # | Bug | 位置 | 后果 |
|---|---|---|---|
| 3 | `typeof window.parent.OpenCamera` 跨域抛异常 | doScan（480-494） | iframe 与 Portal 跨域时访问 `window.parent` 属性抛 SecurityError（`typeof` 不保护），扫码功能崩；需 try/catch |
| 4 | **EXIF 旋转未处理** | compressImage（813-849） | `canvas.drawImage` 不读 EXIF orientation，**手机竖拍照片压缩后横过来** |
| 5 | Portal API 永不回调时页面卡死 | 全部上传/提交调用 | 无超时兜底，loading 永远转、按钮永远"提交中" |
| 6 | 死代码 | `resetAll`（1027-1049）从未调用；`state.selectedItem` 被 selectItem（643）设置后立刻被 removePackingPanel（734）清 null，实际恒为 null；`initConfirmEvent`（1022）空壳 | 误导维护者以为功能存在 |

### 2.3 mom-nameplate-photo-upload（铭牌照片上传页）

| # | Bug | 位置 | 后果 |
|---|---|---|---|
| 1 | **上传竞态：照片串配置** | handleFileSelect（986-1030）+ clearForm（542-558）/ selectStation（513-523） | 上传中清空表单/换工位，回调回来 push 进新 state；若新配置含同 typeCode 则旧照片显示为新配置照片 |
| 2 | **重复提交窗口** | handleSubmit 成功回调（1328-1329 立即复位 submitting 与按钮） | 同 packing，成功 toast 期间可再提交；toast 被移除时回调丢失 |
| 3 | `typeof window.parent.OpenCamera` 跨域 | doScan（705-718） | 同 packing |
| 4 | EXIF 旋转未处理 | compressImage（1040-1075） | 同 packing |
| 5 | `filterStations` 无字段防御 | 481-488 `s.stationName.toLowerCase()` | stationName 缺失 → 抛错 |
| 6 | typeCode 直接拼 jQuery 选择器 | 914, 964 `$("#thumb-list-" + typeCode)` | typeCode 含 `.`/`:` 等特殊字符时选择器失效 |
| 7 | 空配置时强制 show 空卡片 | doQueryPhotoConfig（792-794）在 renderOrderInfo 可能 hide 后又 show | 无订单信息时显示空白区域 |

---

## 3. 维护性问题清单（人在维护时踩的坑）

### 3.1 高

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 1 | **跨项目复制粘贴** | mom-packing/Index.js 与 mom-nameplate-photo-upload/index.js | 12 个函数几乎相同、约 310 行逐字重复（`showToast`/`compressImage`/`escapeHtml`/`buildFieldRow`/`doScan`/`initPage` 等）；两处 `CONFIG` 已分叉（packing 多 `MAX_PHOTOS`）——重复开始腐烂。受 3 文件约束**只能接受保留**，但需在文件头注明"与 XX 页 XX 函数保持同步" |
| 2 | **全局函数裸奔（无 IIFE）** | 全部 JS 文件 | 每页 30+ 顶层函数挂 window。若 Portal 在**同一 iframe 内切换页面**（而非独立 iframe），`state`/`CONFIG`/`showToast` 同名互相覆盖。约束内缓解：对象命名空间（`var PK = {...}`）或函数前缀（`pk_`/`np_`） |
| 3 | **id/类名魔法字符串散落三处** | mom-cert 的 `dp_`/`zc_`/`hb_g6_ry_` 等前缀 | HTML id、JS 选择器、CSS 选择器三处对应，改一个前缀要三处同步，漏一处即静默失效（校验引擎不报错） |
| 4 | **同一页面两套代码风格** | mom-cert inline script vs index.js | 新引擎有 `safeParse`/防御，旧脚本裸 parse + debugger；改校验逻辑时不知改哪套 |
| 5 | **Mock 掩盖生产问题** | 三个页面 | Mock 字段齐全、列表小、无超时——缺字段白屏、大列表卡顿、慢网络竞态全部测不出来；删 Mock 靠手删注释区块，删漏即事故 |
| 6 | 非 git 仓库 | 项目根目录 | 无版本历史，改错无法回滚 |

### 3.2 中

| # | 问题 | 说明 |
|---|---|---|
| 7 | **事件绑定三风格混用** | mom-cert 内联 `onclick` + 直接绑定；packing/nameplate 委托为主但有重复绑定嫌疑（photo-add-btn 两处绑定，靠元素重建规避） |
| 8 | 回调地狱 | `compressImage → upload → render` 三层嵌套，改一处要在括号海里找位置 |
| 9 | 魔法数字 | `3000`/`0.8`/`300`/`800`/`50`/`100`/`150`/`20`/`0.1` 散落，无注释 |
| 10 | 注释与代码脱节 | `stepIndex for tracking`（无此变量）、`@param validateArray`（实际参数名 validateMap）、空壳函数 |
| 11 | 字段拼写错误 | `CarConfirmityCertNo`/`AxleDistince`/`EnterpriceNo`/`Localtion` 等，排查时需先判断"笔误还是后端字段" |
| 12 | 调试残留 | 全项目大量 `console.log("[API] ...")`；mom-cert/index.html 有 3 处 debugger |
| 13 | 超长函数 | nameplate `showPhotoPreview` 148 行（1085-1233）、mom-cert `loadHBAndRYData` 160 行（4256-4415）、`handleSubmit` 106 行 |
| 14 | 死代码 | packing `resetAll`/`initConfirmEvent`/`state.selectedItem` 恒 null |
| 15 | jQuery 走 CDN | 内网访问不了 jsdelivr → 白屏 |

### 3.3 「HTML 全部在 JS 里」——人维护时的具体痛点（用户提出的角度，展开）

1. 字符串拼接无模板感：改 UI 结构要在 `+` 号里数括号、找引号。
2. **转义负担**：每个动态值都要记得 escapeHtml，漏一个就是 XSS（mom-cert 已中招）。而 `escapeHtml` 本身只适用于文本节点——拼进 `attr`、`src`、`style`、`data-*` 时各有各的坑（如模板图 URL 里带引号会炸属性）。
3. git diff 噪音：改一处结构，整行 `'<div>'+...` 都是改动。
4. SVG 图标整段内联在 JS 字符串里，无法复用，改图标要靠"复制新 path"。

---

## 4. 3 文件约束内的结构建议（适度原则）

**不建议**引入 webpack/vite/TypeScript/前端框架；约束内也**无法**新增公共文件。建议的最小改进（全部不动页面架构）：

```
mom-packing/
├── index.html   # 不变
├── index.js     # 页内重构：模板字符串 + 函数归组 + 修 bug
└── index.css    # 不变
```

1. **字符串拼接 → ES6 模板字符串**：可读性提升一个量级，零依赖（需设备内核支持 ES6，工业平板 WebView 一般没问题，上线前需确认）。
2. **`__DEV__` 改运行时判断**：如 `location.href` 带 dev 参数或由 Portal 注入开关，**部署不再改代码**（mom-cert 最值得改的一处）。
3. **函数归组（不用 IIFE）**：`var PK = { state: {...}, showToast: fn, ... }` 或函数前缀 `pk_`，降低同名全局覆盖风险。
4. **每页顶部自带一份 `safeParse` + `escapeHtml`**（各页副本，注释注明同步关系）。
5. **统一错误处理约定**：所有 API 调用带超时兜底 + `state.submitting` 保护 + 提交成功后按钮锁定 3 秒。
6. **上传回调带上下文校验**：记录发起上传时的归属（item.ID / typeCode + 一个 requestId），回调时与当前 state 比对，不一致则丢弃并提示。
7. 跨页约 310 行重复代码**接受保留**（约束决定），文件头注释注明同步关系。

---

## 5. 分三步的实施路线

### 第 1 步：修 bug（半天，收益最大）

1. `git init` + 初始提交（若允许本地建仓）。
2. mom-cert（**仅改 index.js，HTML 不动**）：
   - 顶层新增 `normalizeInputs()`：缺失的可选 JSON 字段补 `''`（`CHECK_CONTENT` 补 `'[]'`）、`MSG` 补 `''`、显示字符串补 `''`——一次归一化消除内联脚本 14 处裸 `JSON.parse` 的崩溃类问题；
   - waitForDOMReady 完成后对 `#check_content` 后置消毒（剔 `script`/`iframe`/`on*` 属性/`javascript:` 链接），消除 XSS；
   - `__DEV__` 改为运行时判断（URL 参数或 Portal 注入开关），部署不用改代码；
   - `debugger;` 在 HTML 内无法从 JS 移除，且无 DevTools 时不生效，按约束忽略。
3. 两新页：上传回调上下文校验（防串单）；提交成功后锁按钮 3 秒 + 成功 toast 期间禁止重复提交；`doScan` 包 try/catch；`compressImage` 处理 EXIF 旋转（可用 `createImageBitmap` 或第三方轻量库，或至少竖拍参数修正）。
4. 清理死代码（`resetAll`/`initConfirmEvent`/`state.selectedItem`）。

### 第 2 步：页内可读性（半天）

1. 字符串拼接 → 模板字符串。
2. 函数归组（对象命名空间或前缀）。
3. 魔法数字收敛为 `CONFIG`。
4. 收敛调试 `console.log`。
5. jQuery 本地化（若内网可达性有问题）。

### 第 3 步：可选（等有需求再做）

- mom-cert 整体重构：HTML 模板化（175 处内联 `display:none` 收敛为数据驱动渲染）、拆分内联脚本、`reloadZHC` 45 对 hide/show 去重为循环。
- 若页面数量继续增长，再评估 CDN 引入 Vue（仍无构建、仍 3 文件，框架本身可以写在 index.js 里）。

---

## 6. 「全部在 JS 中是不是不好维护」——结论

- **UI 由 JS 渲染 ≠ 不好维护**。packing / nameplate 是数据驱动的动态页面，用 JS 渲染是合理选择，且"人觉得费力"主要是**写法**问题而非"JS 渲染"问题。
- **当前写法里最伤维护性的三点**：① 拼接字符串无模板感；② 全局函数与魔法字符串散落；③ 跨页复制粘贴导致"修一处、漏一处"。
- **改进路径不激进**：模板字符串 → 函数归组 → 修 bug。全部在 3 文件约束内可完成。

---

## 7. 亮点（重构时应保留）

- 新项目的事件委托集中管理（nameplate/index.js:561-702 `initEvents()`）。
- 动态数据统一经过 `escapeHtml()`（mom-packing/Index.js:180-188）。
- Mock/生产切换机制及文件头对接说明文档（mom-packing/Index.js:18-45）——但 `__DEV__` 开关建议改运行时判断。
- CSS 变量 + 容器 ID 前缀作用域隔离（mom-packing/index.css:2-20）。
